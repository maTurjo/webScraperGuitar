from selenium import webdriver
from options import options
def listOfSongs(Url:str):
    # driver=webdriver.Chrome(options=options)
    # driver.get(Url)
    # print(driver.title)
    print("list of songs is called")